# Copy to .env.local and set values
OPENAI_API_KEY=sk-...
# Optional: set to a random secret for webhook signature derivation
WEBHOOK_SIGNING_SECRET=replace-with-random-hex
# App URL for webhook registration (e.g., https://yourdomain.com/api/webhook)
APP_BASE_URL=http://localhost:3000
